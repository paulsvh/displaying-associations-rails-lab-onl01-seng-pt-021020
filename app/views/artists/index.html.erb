<h1>Artists</h1>

<ul>
    <% @artists.each do |artist| %>
        <li><%= artist.name + " - " + artist.song_count.to_s +
         " song".pluralize(artist.song_count) %>
        </li>
    <% end %>
</ul>